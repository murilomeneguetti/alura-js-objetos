// Criar um array vazio com 10 posições
let meuArray = new Array(10);

// Verificar se a quarta posição existe (índice 3)
if (meuArray.length > 3) {
  // Preencher somente a quarta posição
  meuArray[3] = "Valor na quarta posição";
}

// Exibir o array
console.log(meuArray);

meuArray[4] = 'teste';

console.log(meuArray);
